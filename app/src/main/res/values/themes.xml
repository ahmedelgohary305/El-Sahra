<resources>
    <style name="Theme.ElSahra" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Material 3 already extends AppCompat, but you need NoActionBar -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.ElSahra</item>
    </style>
</resources>
